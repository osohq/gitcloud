type Job {
  id: ID!
  name: String!
  status: String!
  repoId: ID!
  creatorId: String!
  createdAt: String!
  updatedAt: String!
}

type Query {
  listJobs(repoId: ID!): [Job]!
}
type Mutation {
  createJob(name: String!, repoId: ID!): Job!
  cancelJob(id: ID!): Job!
}
